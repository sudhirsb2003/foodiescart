<%= form_for(@ingredient_recipe) do |f| %>
  <% if @ingredient_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient_recipe.errors.count, "error") %> prohibited this ingredient_recipe from being saved:</h2>

      <ul>
      <% @ingredient_recipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipe_id %><br />
    <%= f.number_field :recipe_id %>
  </div>
  <div class="field">
    <%= f.label :ingredient_id %><br />
    <%= f.number_field :ingredient_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
