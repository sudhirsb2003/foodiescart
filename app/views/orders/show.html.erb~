
<style>
@font-face { font-family: "WebRupee"; font-style: normal; font-weight: normal; src: local("WebRupee"), url("http://cdn.webrupee.com/WebRupee.V2.0.ttf") format("truetype"), url("http://cdn.webrupee.com/WebRupee.V2.0.woff") format("woff"), url("http://cdn.webrupee.com/WebRupee.V2.0.svg") format("svg"); } .WebRupee { font-family: 'WebRupee'; }
</style>

<p>
  <b>Ip adress:</b>
  <%= @order.ip_adress %>
</p>

<p>
  <b>First name:</b>
  <%= @order.first_name %>
</p>

<p>
  <b>Last name:</b>
  <%= @order.last_name %>
</p>

<p>
  <b>Card name:</b>
  <%= @order.card_name %>
</p>

<p>
  <b>Card type:</b>
  <%= @order.card_type %>
</p>

<p>
  <b>Card Number:</b>
  <%= @order.card_number %>
</p>

<p>
  <b>Card expires on:</b>
  <%= @order.card_expires_on %>
</p>

<p>
  <b>Products:</b>

<table id="line_items">
  <tr>
    <td>Product</td>
    <td>Brand</td>
    <td>Qty</td>
    <td class="price">Unit Price</td>
    <td class="price">Full Price</td>
    <td class="price">Commision</td>
  </tr>
  <% for line_item in @order.cart.line_items %>
    <tr class="<%= cycle :odd, :even %>">
     <td><%=h line_item.product.ingredient.name %></td>
     <td><%=h line_item.product.brand.name %></td>
     <td class="qty"><%= line_item.quantity %></td>
     <td class="price"><%= number_to_currency(line_item.product.price) %></td>
     <td class="price"><%= number_to_currency(line_item.total_price) %></td>
     <td class="price"><%= number_to_currency(((line_item.product.price) *(line_item.product.commision/100))*(line_item.quantity)) %></td>
     <%= @order.commision %>
    </tr>
  <% end %>

  <tr>
    <td class="total price" colspan="4">
      <br><b style ="margin-left:395px"> Total: <%= number_to_currency @order.cart.total_price %> </b>
    </td>
  </tr>

</table>

</p>



<%= link_to 'BUY MORE', products_path %>
